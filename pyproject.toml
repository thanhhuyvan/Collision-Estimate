[project]
name = "guardian-perception"
version = "0.1.0"
description = "Detector-neutral, vision-only forward-collision warning core."
requires-python = ">=3.11"

[project.optional-dependencies]
yolo = ["ultralytics"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
